var express = require('express'),
    router = express.Router();
/*
router.get("/", function (req, res) {
    res.send("hello there! we will delete this once we start playing with angular but i guess you'll be anxious to see some results by now so here you go")
})*/

/*
    in case if you're wondering why we need this if it's gonna get deleted...
    well, not all of this will be deleted, just that part above, we'll still use routes
    for authentication or in case if we want to create some kind of API, i'll most
    likely show you that too
*/

module.exports = router;